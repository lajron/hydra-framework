# Knowledge Space Templates

These templates define the default shape for repository areas that need durable
local knowledge. Copy only the files a knowledge space needs, then replace placeholders.

The templates encode reusable defaults: scoped reading maps, context packs,
small prompt-routing metadata, node-local validation, honest
planned-versus-verified status, architecture-graph navigation, and explicit
definition-of-done files.
